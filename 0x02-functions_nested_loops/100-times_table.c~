#include "main.h"
/**
*  print_times_table - discreption
*@n: takes 
* Return: Always 0.
*/
void print_times_table(int n)
{
  int prod, mult, num;
  if (n <= 15 && n >= 0)
    {
      for (num = 0; num <= n; ++num)
	{
	  _putchar(48);
	  for (mult =1; mult <= n; ++mult
